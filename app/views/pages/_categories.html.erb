<div class="widget widget_categories">
	<h3 class="widget_title">Categories</h3>
	<ul>
		<% ActsAsTaggableOn::Tag.most_used(10).each do |tag| %>
    <%= content_tag(:li, link_to(tag.name, category_path(tag.name))) %>
    <% end %>
	</ul>
</div>