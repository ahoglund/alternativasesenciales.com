<div class="widget widget_categories">
	<h3 class="widget_title">Categories</h3>
	<ul>
		<% categories.each do |tag| %>
    <%= content_tag(:li, link_to(tag.name.capitalize, category_path(tag.name))) %>
    <% end %>
	</ul>
</div>